#include <stdio.h>

/**
 * main - prints alphabet lowercase reverse
 *
 * Return: 0 (all success)
 */

int main(void)
{
	int n;

	for (n = 122; n > 96; n--)
		putchar(n);
	putchar(10);

	return (0);
}
