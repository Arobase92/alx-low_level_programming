#include <string.h>
#include "main.h"
/**
 * _strlen - compute the length of string
 * @s: the string
 *
 * Return: the length to s
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
