#include <string.h>
#include "main.h"

/**
 * _strcpy - copy the string
 *
 * @dest: destination
 * @src: source
 *
 * Return: pointer char
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
